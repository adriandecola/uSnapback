{
  "name": "usnapback",
  "version": "1.0.0",
  "type": "module",
  "description": "An application to design a snapback primer for single base variant asymmetrical PCR",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "node build.js",
    "serve": "vite",
    "serve:silent": "TEST=true vite",
    "test": "npm run build && concurrently -k -s first -n SERVE,TEST \"npm run serve:silent\" \"wait-on http://localhost:8000 && node --experimental-vm-modules node_modules/.bin/jest\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^30.0.3",
    "jest-environment-jsdom": "^30.0.2",
    "jsdom": "^26.1.0",
    "node-fetch": "^3.3.2",
    "vite": "^7.0.6"
  },
  "dependencies": {
    "dotenv": "^17.2.1"
  }
}
